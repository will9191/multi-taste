const BASE_URL = 'http://localhost:8080';

export const ApiEndpoint = {
  Category: {
    base: `${BASE_URL}/category`,
  },
  Ingredient: {
    base: `${BASE_URL}/ingredient`,
  },
  Product: {
    base: `${BASE_URL}/product`,
  },
};
